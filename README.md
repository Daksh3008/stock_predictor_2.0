# Price Forecast POC - Phase 0

Phase 0 repo: reproducible, deterministic ensemble forecasting baseline
(4 models + ensemble, feature pipeline, SHAP explainability).

## Quick start

1. Create venv & install:
   ```bash
   python -m venv venv
   venv\Scripts\activate   # Windows
   pip install -r requirements.txt
